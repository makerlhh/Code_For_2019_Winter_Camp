#include <iom16v.h>

void delay(unsigned int ms)//ms¼¶ÑÓÊ±º¯Êı
{
   int i;
   for(i=0;i<ms*1148;i++);
}

void main()
{
    int i,j;
    DDRA = 0xFF;
	DDRC = 0xFF;
	PORTC = ~0x01;
	for(i=0;i<8;i++)
	{
	    PORTA = 0x01;
	    for(j=0;j<7;j++)
	    {
		    delay(500);
		    PORTA = (PORTA << 1);
		}
		delay(500);
		PORTC = (PORTC << 1) + 1;
	}
}